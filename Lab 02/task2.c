#include <stdio.h>
int main()
{
    int h = 7, w = 5;
    int area = h * w;
    int perimeter = 2 * (h + w);

    printf("Area: %d\n", area);
    printf("Perimeter: %d\n", perimeter);

    return 0;
}